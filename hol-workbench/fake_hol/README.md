# Synthetic protocol fixtures

These helpers simulate protocol events for Python tests. They do not evaluate
HOL, establish a theorem, or provide a fallback when a proof runtime is missing.
The public demos use real HOL Light through an existing warm profile.
