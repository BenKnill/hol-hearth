#!/bin/bash
set -euo pipefail

script_path="${BASH_SOURCE[0]}"
script_dir="${script_path%/*}"
if [[ "${script_dir}" == "${script_path}" ]]; then
  script_dir=.
fi
script_dir="$(cd "${script_dir}" && pwd -P)"
exec "${script_dir}/workbench-python" inspect "$@"
